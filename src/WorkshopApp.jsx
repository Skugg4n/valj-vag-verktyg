// Placeholder — replaced with the real workshop editor in the next task.
export default function WorkshopApp() {
  return <div style={{ padding: 24, fontFamily: 'sans-serif' }}>Workshop (bygger…)</div>
}
